# Local development database configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:opossum}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres123}

# Security configuration
cors.urls=${CORS_URLS:http://localhost:3000}
cors.enabled=${CORS_ENABLED:true}
csrf.enabled=${CSRF_ENABLED:false}
samesite.cookie.enabled=${SAMESITE_COOKIE_ENABLED:false}

# JWT configuration
jwt.secret=${JWT_SECRET:rKkTeex9w6EG9q4A4AGkpvgoz2DwP0wM6KhRWAK7fHw=}
jwt.expiration=${JWT_EXPIRATION:86400000}